% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mdpde_bccg.R
\name{mdpde_bccg}
\alias{mdpde_bccg}
\title{Fit a BCCG Regression Model via the MDPDE}
\usage{
mdpde_bccg(
  formula,
  sigma.formula = ~1,
  nu.formula = ~1,
  alpha = 0.1,
  data,
  mu.link = "log",
  sigma.link = "log",
  nu.link = "identity"
)
}
\arguments{
\item{formula}{A formula for the median sub-model (e.g. \code{y ~ x1 + x2}).}

\item{sigma.formula}{A one-sided formula for the CV sub-model (default \code{~1}).}

\item{nu.formula}{A one-sided formula for the skewness sub-model
(default \code{~1}).}

\item{alpha}{Non-negative tuning parameter (default 0.1).  \code{alpha = 0}
returns an MLE fit via GAMLSS.}

\item{data}{A data frame.}

\item{mu.link}{Link function for mu: \code{"log"} (default), \code{"identity"}, or
\code{"sqrt"}.}

\item{sigma.link}{Link function for sigma: \code{"log"} (default), \code{"identity"},
or \code{"sqrt"}.}

\item{nu.link}{Link function for nu: \code{"identity"} (default), \code{"log"}, or
\code{"sqrt"}.}
}
\value{
An object of class \code{"mdpde_bccg"}, a list with components:
\describe{
\item{coefficients}{Named list with entries \code{mu}, \code{sigma}, \code{nu}.}
\item{se}{Asymptotic standard errors (sandwich), same structure.}
\item{t_values, p_values}{Wald statistics and two-sided p-values.}
\item{fitted.values}{Data frame with columns \code{mu}, \code{sigma}, \code{nu}.}
\item{residuals}{Randomised quantile residuals.}
\item{weights}{Robust weights \eqn{w_i = f(y_i; \hat\theta)^\alpha}.}
\item{convergence}{Integer from \code{\link[stats:optim]{stats::optim()}} (0 = success).}
\item{cov_matrix}{Full \eqn{p \times p} sandwich covariance matrix.}
\item{alpha, n, links, formula, sigma.formula, nu.formula, data, y}{
Model metadata.}
\item{value}{Final objective value.}
\item{call}{Matched call.}
}
}
\description{
Estimates parameters of the Box-Cox Cole and Green (BCCG) regression model
using the Minimum Density Power Divergence Estimator (MDPDE) proposed by
Basu et al. (1998).  The tuning parameter \code{alpha} controls the robustness-
efficiency trade-off: \code{alpha = 0} reduces to MLE (handled via
\code{\link[gamlss:gamlss]{gamlss::gamlss()}}), while larger values increase robustness at the cost of
efficiency.
}
\examples{
\dontrun{
set.seed(42)
n <- 100
x <- runif(n)
mu_true <- exp(3 + x)
y <- gamlss.dist::rBCCG(n, mu = mu_true, sigma = 0.1, nu = 2)
dat <- data.frame(y = y, x = x)

# Robust fit
fit_rob <- mdpde_bccg(y ~ x, data = dat, alpha = 0.1)
summary(fit_rob)

# MLE (alpha = 0)
fit_mle <- mdpde_bccg(y ~ x, data = dat, alpha = 0)
}

}
\references{
Basu, A., Harris, I. R., Hjort, N. L., & Jones, M. C. (1998).
Robust and efficient estimation by minimising a density power divergence.
\emph{Biometrika}, \strong{85}(3), 549–559. \doi{10.1093/biomet/85.3.549}

Cole, T. J., & Green, P. J. (1992). Smoothing reference centile curves: the
LMS method and penalized likelihood. \emph{Statistics in Medicine}, \strong{11}(10),
1305–1319.
}
\seealso{
\code{\link[=select_alpha_mdpde]{select_alpha_mdpde()}}, \code{\link[=mdpde_diagnostics]{mdpde_diagnostics()}}, \code{\link[=summary.mdpde_bccg]{summary.mdpde_bccg()}}
}
