% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_alpha.R
\name{select_alpha_mdpde}
\alias{select_alpha_mdpde}
\title{Select the Optimal MDPDE Tuning Parameter Alpha}
\usage{
select_alpha_mdpde(
  formula,
  sigma.formula = ~1,
  nu.formula = ~1,
  data,
  alphas = seq(0, 0.35, 0.05),
  trim = 0.05,
  epsilon = 0.05,
  only_converged = TRUE,
  mu.link = "log",
  sigma.link = "log",
  nu.link = "identity",
  refine = TRUE,
  seed = 123
)
}
\arguments{
\item{formula, sigma.formula, nu.formula}{Formulas passed to \code{\link[=mdpde_bccg]{mdpde_bccg()}}.}

\item{data}{A data frame.}

\item{alphas}{Numeric vector of candidate alpha values for Phase 1
(default \code{seq(0, 0.35, 0.05)}).}

\item{trim}{Trimming proportion for the LVNMP criterion; observations with
the \code{trim * 100}\% largest negative log-likelihood contributions are
excluded (default 0.05).}

\item{epsilon}{Minimum relative weighted NLL improvement required to prefer
the robust model over MLE (default 0.05, i.e. 5\%).}

\item{only_converged}{If \code{TRUE} (default), only converged fits are eligible
as optimal.}

\item{mu.link, sigma.link, nu.link}{Link functions passed to \code{\link[=mdpde_bccg]{mdpde_bccg()}}.}

\item{refine}{Logical; whether to run Phase 2 local refinement
(default \code{TRUE}).}

\item{seed}{Numeric seed for reproducibility. Default is 123. Set to \code{NULL} to skip setting seed.}
}
\value{
A named list with:
\describe{
\item{fit}{The final \code{\link[=mdpde_bccg]{mdpde_bccg()}} object for the optimal alpha.}
\item{optimal_alpha}{The selected alpha value.}
\item{theoretical_min_alpha}{The alpha that minimised LVNMP before the
threshold rule.}
\item{alpha_skipped}{Logical; \code{TRUE} if the theoretical minimum was
replaced due to non-convergence.}
\item{nll_improvement}{Relative weighted NLL improvement of the robust
model over MLE.}
\item{table}{Data frame with columns \code{alpha}, \code{TrimError}, \code{Type},
\code{Converged}, \code{Category}.}
}
}
\description{
Implements the two-phase grid-search and local-refinement procedure described
in Section 3.2 of the TCC, selecting the value of \code{alpha} that minimises the
Trimmed Mean Negative Log-Likelihood (LVNMP) across the data.
}
\details{
\strong{Phase 1 – Global grid search:} fits the MDPDE for every candidate in
\code{alphas} and maps the LVNMP surface.

\strong{Phase 2 – Local refinement:} fits a finer grid around the Phase-1 minimum
(step size 0.01) when \code{refine = TRUE}.

\strong{Acceptance threshold:} the optimal alpha is forced to zero (MLE) when the
weighted NLL improvement over MLE is below \code{epsilon} (Section 3.2.3).
}
\examples{
\dontrun{
set.seed(1)
n <- 120
x <- runif(n)
y <- gamlss.dist::rBCCG(n, mu = exp(3 + x), sigma = 0.1, nu = 2)
dat <- data.frame(y = y, x = x)

sel <- select_alpha_mdpde(formula = y ~ x, data = dat)
cat("Optimal alpha:", sel$optimal_alpha, "\n")
summary(sel$fit)
}

}
\references{
Hadi, A. S., & Luceno, A. (1997). Maximum trimmed likelihood estimators:
a unified approach, examples, and algorithms.
\emph{Computational Statistics & Data Analysis}, \strong{25}(3), 251–272.
}
\seealso{
\code{\link[=mdpde_bccg]{mdpde_bccg()}}, \code{\link[=mdpde_diagnostics]{mdpde_diagnostics()}}
}
