% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{mdpde_diagnostics}
\alias{mdpde_diagnostics}
\title{Four-panel Diagnostic Plot for an mdpde_bccg Fit}
\usage{
mdpde_diagnostics(model, label = "all", weight_cutoff = 0.05)
}
\arguments{
\item{model}{An object of class \code{"mdpde_bccg"}.}

\item{label}{Labelling of potential outliers. One of:
\itemize{
\item \code{"all"} (default): label all observations with \code{weight < weight_cutoff}.
\item \code{"none"} or \code{FALSE}: no labels.
\item A numeric vector of observation indices to label.
}}

\item{weight_cutoff}{Weight threshold below which an observation is
considered a potential outlier (default 0.05).}
}
\value{
Invisibly, a list with \code{residuals} and \code{weights}. The function is
called for its side effect (a combined \code{gridExtra} plot).
}
\description{
Produces four complementary diagnostic graphics based on the randomised
quantile residuals of the fitted model:
}
\details{
\enumerate{
\item \strong{Residuals vs Fitted Values} – checks homogeneity of variance.
\item \strong{Residuals vs Index} – checks independence / sequential trends.
\item \strong{Kernel Density of Residuals} – checks normality of the residuals.
\item \strong{Normal Q-Q Plot} – checks normality; the reference line is fitted
robustly from the inter-quartile range to avoid distortion by outliers.
}

Under a correctly specified model, quantile residuals follow N(0, 1).
Points coloured red have robust weights below \code{weight_cutoff} and are
flagged as potential outliers by the estimation procedure.
}
\examples{
\dontrun{
fit <- mdpde_bccg(y ~ x, data = dat, alpha = 0.15)
mdpde_diagnostics(fit)
}

}
\seealso{
\code{\link[=mdpde_wp]{mdpde_wp()}}, \code{\link[=mdpde_weights]{mdpde_weights()}}, \code{\link[=mdpde_bccg]{mdpde_bccg()}}
}
